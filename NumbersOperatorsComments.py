# ints(whole numbers +/-) floats(decimals)
# power(**)
# modulo(%) remainder operator, divide as many times as possible, output is that remains
# interger division(//) do the devision, give interget(whole number rounded down) as result
print(1 + 2)
# test
